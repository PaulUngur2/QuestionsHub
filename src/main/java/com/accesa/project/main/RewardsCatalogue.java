package com.accesa.project.main;

import com.accesa.project.user.User;

import java.util.Map;

public class RewardsCatalogue {
    private Map<String, Integer> rewards;

    public void addReward(String name, int cost) {
        // add a reward to the catalog with the specified name and cost
    }

    public void redeemReward(User user, String rewardName) {
        // allow the user to redeem the reward with the specified name if they have enough tokens/badges
    }

    // getters and setters
}
